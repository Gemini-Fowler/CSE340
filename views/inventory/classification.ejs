<!-- Content Heading Section -->
<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Message Block (disabled for now; enable in future unit) -->
<%# messages() %>

<!-- Inventory Items Display Section -->
<%- grid %>
